import { ProjectsGrid } from "@/widgets/projects-grid/ProjectsGrid";

export const metadata = {
  title: "Projects | Kyrillos Samer",
  description: "Front-End & GIS projects by Kyrillos Samer",
};

export default function ProjectsPage() {
  return <ProjectsGrid />;
}